<style>
    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
    }

    #login-usuario-container {
        margin: 0;
        padding: 20px;
        background: linear-gradient(135deg, #f5f7fa 0%, #dfe3f0 100%);
        font-family: 'Segoe UI', Roboto, 'Helvetica Neue', sans-serif;
        min-height: 100vh;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        position: relative;
        overflow: hidden;
    }

    #login-usuario-container::before {
        content: '';
        position: absolute;
        top: -50px;
        right: -50px;
        width: 300px;
        height: 300px;
        background: rgba(59, 77, 156, 0.08);
        border-radius: 50%;
        z-index: 0;
    }

    #login-usuario-container::after {
        content: '';
        position: absolute;
        bottom: -80px;
        left: -80px;
        width: 400px;
        height: 400px;
        background: rgba(59, 77, 156, 0.05);
        border-radius: 50%;
        z-index: 0;
    }

    .login-content {
        position: relative;
        z-index: 1;
        max-width: 500px;
        padding: 50px;
        background: rgba(255, 255, 255, 0.98);
        border-radius: 24px;
        box-shadow: 0 15px 40px rgba(0, 0, 0, 0.08);
        width: 100%;
    }

    .login-header {
        text-align: center;
        margin-bottom: 40px;
    }

    .login-title {
        font-size: 28px;
        color: #2a3a7a;
        font-weight: 600;
        margin-bottom: 10px;
        position: relative;
        padding-bottom: 15px;
    }

    .login-title::after {
        content: '';
        position: absolute;
        bottom: 0;
        left: 50%;
        transform: translateX(-50%);
        width: 60px;
        height: 3px;
        background: linear-gradient(90deg, #3b4d9c, #6a82eb);
        border-radius: 3px;
    }

    .login-subtitle {
        font-size: 16px;
        color: #6b7280;
        font-weight: 400;
    }

    .login-form {
        display: flex;
        flex-direction: column;
        gap: 25px;
    }

    .form-group {
        display: flex;
        flex-direction: column;
    }

    .form-label {
        font-size: 15px;
        color: #2a3a7a;
        margin-bottom: 8px;
        font-weight: 500;
    }

    .form-input {
        padding: 15px 20px;
        border: 1px solid #dfe3f0;
        border-radius: 12px;
        font-size: 15px;
        transition: all 0.3s ease;
        background-color: #f9fafc;
    }

    .form-input:focus {
        outline: none;
        border-color: #3b4d9c;
        box-shadow: 0 0 0 3px rgba(59, 77, 156, 0.1);
    }

    .forgot-password {
        text-align: right;
        margin-top: 5px;
    }

    .forgot-password a {
        color: #6b7280;
        font-size: 13px;
        text-decoration: none;
        transition: color 0.2s ease;
        font-weight: 500;
    }

    .forgot-password a:hover {
        color: #3b4d9c;
        text-decoration: underline;
    }

    .button-container {
        display: flex;
        gap: 15px;
        margin-top: 10px;
    }

    .login-button {
        flex: 1;
        padding: 16px;
        border-radius: 12px;
        font-size: 15px;
        font-weight: 600;
        cursor: pointer;
        transition: all 0.3s ease;
        text-align: center;
        text-decoration: none;
    }

    .primary-button {
        background: linear-gradient(135deg, #3b4d9c 0%, #6a82eb 100%);
        color: white;
        border: none;
        box-shadow: 0 4px 15px rgba(59, 77, 156, 0.2);
    }

    .primary-button:hover {
        transform: translateY(-2px);
        box-shadow: 0 6px 20px rgba(59, 77, 156, 0.3);
    }

    .secondary-button {
        background: white;
        color: #3b4d9c;
        border: 1px solid #dfe3f0;
    }

    .secondary-button:hover {
        background: #f5f7fa;
    }

    .login-footer {
        margin-top: 30px;
        color: #6b7280;
        font-size: 14px;
        text-align: center;
        width: 100%;
    }

    .footer-links {
        display: flex;
        justify-content: center;
        gap: 15px;
        margin-top: 15px;
        flex-wrap: wrap;
    }

    .footer-link {
        color: #3b4d9c;
        text-decoration: none;
        font-weight: 500;
        transition: all 0.2s ease;
    }

    .footer-link:hover {
        color: #6a82eb;
        text-decoration: underline;
    }

    @media (max-width: 768px) {
        .login-content {
            padding: 40px;
        }

        .login-title {
            font-size: 24px;
        }
    }

    @media (max-width: 480px) {
        .login-content {
            padding: 30px;
            border-radius: 16px;
        }

        .login-title {
            font-size: 22px;
        }

        .button-container {
            flex-direction: column;
        }

        .footer-links {
            gap: 10px;
        }
    }
</style>

<main id="login-usuario-container">
    <div class="login-content">
        <div class="login-header">
            <h1 class="login-title">Acesse sua conta</h1>
            <p class="login-subtitle">Informe seus dados para continuar</p>
        </div>

        <form class="login-form" id="login-usuario-form" action="/usuarios/logar-usuario" method="post">
            <div class="form-group">
                <label for="login-usuario-email" class="form-label">E-mail</label>
                <input type="email" id="login-usuario-email" name="email" class="form-input" placeholder="seu@email.com"
                    required>
            </div>

            <div class="form-group">
                <label for="login-usuario-senha" class="form-label">Senha</label>
                <input type="password" id="login-usuario-senha" name="senha" class="form-input" placeholder="••••••••"
                    required>
                <div class="forgot-password">
                    <a href="/recuperar-senha">Esqueceu sua senha?</a>
                </div>
            </div>

            <div class="button-container">
                <button type="submit" class="login-button primary-button">Entrar</button>
                <a href="/cadastro-usuarios" class="login-button secondary-button">Criar conta</a>
            </div>
        </form>

        <div class="login-footer">
            <p>Não possui uma conta?</p>
            <div class="footer-links">
                <a href="/cadastro-usuarios" class="footer-link">Crie a sua conta</a>
                <a href="/" class="footer-link">Voltar à página inicial</a>
            </div>
        </div>
    </div>
</main>