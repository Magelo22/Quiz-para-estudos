<style>
  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  #cadastro-page {
    margin: 0;
    padding: 20px;
    background: linear-gradient(135deg, #f5f7fa 0%, #dfe3f0 100%);
    font-family: 'Segoe UI', Roboto, 'Helvetica Neue', sans-serif;
    min-height: 100vh;
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
  }

  #cadastro-page::before {
    content: '';
    position: absolute;
    top: -100px;
    right: -100px;
    width: 400px;
    height: 400px;
    background: rgba(106, 130, 235, 0.1);
    border-radius: 50%;
    z-index: 0;
  }

  #cadastro-page::after {
    content: '';
    position: absolute;
    bottom: -150px;
    left: -150px;
    width: 500px;
    height: 500px;
    background: rgba(106, 130, 235, 0.08);
    border-radius: 50%;
    z-index: 0;
  }

  .cadastro-container {
    position: relative;
    z-index: 1;
    color: #2a3a7a;
    display: flex;
    flex-direction: column;
    align-items: center;
    width: 100%;
    max-width: 500px;
    padding: 50px;
    background: rgba(255, 255, 255, 0.98);
    border-radius: 24px;
    box-shadow: 0 25px 50px rgba(0, 0, 0, 0.15);
  }

  .cadastro-header {
    text-align: center;
    margin-bottom: 40px;
    width: 100%;
  }

  .cadastro-title {
    font-size: 28px;
    color: #2a3a7a;
    font-weight: 700;
    margin-bottom: 10px;
    position: relative;
    padding-bottom: 15px;
  }

  .cadastro-title::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 80px;
    height: 4px;
    background: linear-gradient(90deg, #3b4d9c, #6a82eb);
    border-radius: 4px;
  }

  .cadastro-subtitle {
    font-size: 16px;
    color: #6b7280;
    font-weight: 400;
  }

  .cadastro-form {
    display: flex;
    flex-direction: column;
    gap: 25px;
    width: 100%;
  }

  .cadastro-input-group {
    display: flex;
    flex-direction: column;
    position: relative;
  }

  .cadastro-label {
    font-size: 15px;
    color: #2a3a7a;
    margin-bottom: 8px;
    font-weight: 600;
  }

  .cadastro-input {
    padding: 16px 20px;
    border: 1px solid #dfe3f0;
    border-radius: 12px;
    font-size: 15px;
    transition: all 0.3s ease;
    background-color: #f9fafc;
    color: #2a3a7a;
  }

  .cadastro-input:focus {
    outline: none;
    border-color: #6a82eb;
    box-shadow: 0 0 0 3px rgba(106, 130, 235, 0.1);
  }

  .cadastro-papel-btn {
    padding: 16px 20px;
    border: 1px solid #dfe3f0;
    border-radius: 12px;
    font-size: 15px;
    transition: all 0.3s ease;
    background-color: #f9fafc;
    color: #2a3a7a;
    appearance: none;
    background-image: url("data:image/svg+xml;charset=UTF-8,%3csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 24 24' fill='%232a3a7a'%3e%3cpath d='M7 10l5 5 5-5z'/%3e%3c/svg%3e");
    background-repeat: no-repeat;
    background-position: right 20px center;
    background-size: 15px;
    cursor: pointer;
    font-weight: 500;
  }

  .cadastro-papel-btn:focus {
    outline: none;
    border-color: #6a82eb;
    box-shadow: 0 0 0 3px rgba(106, 130, 235, 0.1);
  }

  .cadastro-button-group {
    margin-top: 10px;
    width: 100%;
  }

  .cadastro-btn {
    width: 100%;
    padding: 16px;
    border-radius: 12px;
    font-size: 15px;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.3s ease;
  }

  .primary-btn {
    background: linear-gradient(135deg, #3b4d9c 0%, #6a82eb 100%);
    color: white;
    border: none;
    box-shadow: 0 4px 15px rgba(59, 77, 156, 0.2);
  }

  .primary-btn:hover {
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(59, 77, 156, 0.3);
  }

  .secondary-btn {
    background: white;
    color: #3b4d9c;
    border: 1px solid #dfe3f0;
    margin-top: 15px;
  }

  .secondary-btn:hover {
    background: #f5f7fa;
  }

  .cadastro-footer {
    margin-top: 30px;
    color: #6b7280;
    font-size: 14px;
    text-align: center;
    width: 100%;
  }

  .footer-links {
    display: flex;
    justify-content: center;
    gap: 15px;
    margin-top: 15px;
    flex-wrap: wrap;
  }

  .footer-link {
    color: #3b4d9c;
    text-decoration: none;
    font-weight: 500;
    transition: all 0.2s ease;
  }

  .footer-link:hover {
    color: #6a82eb;
    text-decoration: underline;
  }

  @media (max-width: 768px) {
    .cadastro-container {
      padding: 40px;
    }

    .cadastro-title {
      font-size: 24px;
    }
  }

  @media (max-width: 480px) {
    .cadastro-container {
      padding: 30px;
      border-radius: 16px;
    }

    .cadastro-title {
      font-size: 22px;
    }

    .footer-links {
      gap: 10px;
      flex-direction: column;
    }
  }
</style>

<main id="cadastro-page">
  <div class="cadastro-container">
    <div class="cadastro-header">
      <h1 class="cadastro-title">Crie sua conta</h1>
      <p class="cadastro-subtitle">Preencha seus dados para começar</p>
    </div>

    <form class="cadastro-form" id="cadastro-form" action="/usuarios/inserir-usuarios" method="post">
      <div class="cadastro-input-group">
        <label for="cadastro-nome" class="cadastro-label">Nome completo</label>
        <input type="text" id="cadastro-nome" name="nome" class="cadastro-input" placeholder="Digite seu nome completo"
          required>
      </div>

      <div class="cadastro-input-group">
        <label for="cadastro-email" class="cadastro-label">E-mail</label>
        <input type="email" id="cadastro-email" name="email" class="cadastro-input" placeholder="seu@email.com"
          required>
      </div>

      <div class="cadastro-input-group">
        <label for="cadastro-senha" class="cadastro-label">Senha</label>
        <input type="password" id="cadastro-senha" name="senha" class="cadastro-input" placeholder="••••••••" required>
      </div>

      <div class="cadastro-input-group">
        <label for="cadastro-papel" class="cadastro-label">Tipo de conta</label>
        <select class="cadastro-papel-btn" id="cadastro-papel" name="papel" required>
          <option value="" disabled selected>Selecione seu perfil</option>
          <option value="aluno">Aluno</option>
          <option value="professor">Professor</option>
        </select>
      </div>

      <div class="cadastro-button-group">
        <button type="submit" class="cadastro-btn primary-btn">Cadastrar</button>
      </div>
    </form>

    <div class="cadastro-footer">
      <p>Já possui uma conta?</p>
      <div class="footer-links">
        <a href="/login-usuario" class="footer-link">Acessar minha conta</a>
        <a href="/" class="footer-link">Voltar à página inicial</a>
      </div>
    </div>
  </div>
</main>