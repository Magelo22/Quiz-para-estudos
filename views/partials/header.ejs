<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        .header {
            margin: 0;
            font-family: 'Segoe UI', Roboto, 'Helvetica Neue', sans-serif;
            position: relative;
            z-index: 1000;
        }

        .navbar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            background-color: rgba(255, 255, 255, 0.98);
            padding: 0 40px;
            box-shadow: 0 2px 30px rgba(59, 77, 156, 0.08);
            position: relative;
            height: 70px;
        }

        .logo {
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .logo-icon {
            width: 28px;
            height: 28px;
            fill: #3b4d9c;
        }

        .logo-text {
            color: #3b4d9c;
            font-weight: 700;
            font-size: 18px;
            letter-spacing: 0.5px;
        }

        .nav-links {
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .nav-item {
            position: relative;
            padding: 25px 0;
        }

        .nav-links a {
            text-decoration: none;
            color: #2a3a7a;
            font-weight: 500;
            font-size: 15px;
            transition: all 0.3s ease;
            padding: 8px 12px;
            border-radius: 6px;
            display: flex;
            align-items: center;
            gap: 6px;
        }

        .nav-links a:hover {
            color: #6a82eb;
            background-color: rgba(106, 130, 235, 0.05);
        }

        .nav-links a svg {
            width: 16px;
            height: 16px;
            fill: currentColor;
            transition: transform 0.3s ease;
        }

        .logo-img {
            height: 40px;
            width: auto;
            transition: transform 0.3s ease;
        }

        .logo:hover .logo-img {
            transform: scale(1.05);
        }

        .nav-links a:hover svg {
            transform: translateY(-2px);
        }

        .nav-button {
            background: linear-gradient(135deg, #3b4d9c 0%, #6a82eb 100%);
            color: white;
            border: none;
            padding: 10px 22px;
            border-radius: 8px;
            font-weight: 600;
            font-size: 14px;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 2px 10px rgba(59, 77, 156, 0.15);
            display: flex;
            align-items: center;
            gap: 6px;
        }

        .nav-button:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 15px rgba(59, 77, 156, 0.25);
            background: linear-gradient(135deg, #34468a 0%, #5f77df 100%);
        }

        .nav-button svg {
            width: 14px;
            height: 14px;
            fill: white;
        }

        .nav-item.active a {
            color: #3b4d9c;
            font-weight: 600;
        }

        .nav-item.active::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 100%;
            height: 3px;
            background: linear-gradient(90deg, #3b4d9c, #6a82eb);
            border-radius: 3px 3px 0 0;
        }

        @media (max-width: 992px) {
            .navbar {
                padding: 0 25px;
            }

            .nav-links {
                gap: 10px;
            }
        }

        @media (max-width: 768px) {
            .navbar {
                padding: 0 20px;
                height: 60px;
            }

            .logo-text {
                display: none;
            }

            .nav-links a span {
                display: none;
            }

            .nav-links a {
                padding: 10px;
            }

            .nav-links a svg {
                width: 18px;
                height: 18px;
            }

            .nav-button {
                padding: 10px;
                border-radius: 50%;
                width: 36px;
                height: 36px;
                justify-content: center;
            }

            .nav-button span {
                display: none;
            }

            .logo-img {
                height: 30px;
            }
        }
    </style>
</head>

<body>
    <header class="header">
        <nav>
            <div class="navbar">
                <div class="logo">
                    <img src="/images/logo.png" alt="Logo da Empresa" class="logo-img">
                    <span class="logo-text">InforQuiz</span>
                </div>

                <div class="nav-links">
                    <div class="nav-item">
                        <a href="/explorar">
                            <svg viewBox="0 0 24 24">
                                <path
                                    d="M12 10.9c-.6 0-1.1.5-1.1 1.1s.5 1.1 1.1 1.1 1.1-.5 1.1-1.1-.5-1.1-1.1-1.1zm0-5.9c-3.9 0-7 3.1-7 7 0 1.4.4 2.7 1.1 3.8l-3.8 3.8 1.4 1.4 3.8-3.8c1.1.7 2.4 1.1 3.8 1.1 3.9 0 7-3.1 7-7s-3.1-7-7-7zm0 12.3c-2.9 0-5.3-2.4-5.3-5.3s2.4-5.3 5.3-5.3 5.3 2.4 5.3 5.3-2.4 5.3-5.3 5.3z" />
                            </svg>
                            <span>Explorar</span>
                        </a>
                    </div>

                    <div class="nav-item">
                        <a href="/criar-quiz">
                            <svg viewBox="0 0 24 24">
                                <path d="M19 13h-6v6h-2v-6H5v-2h6V5h2v6h6v2z" />
                            </svg>
                            <span>Criar Quiz</span>
                        </a>
                    </div>

                    <div class="nav-item">
                        <a href="/sobre-nos">
                            <svg viewBox="0 0 24 24">
                                <path
                                    d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm1 15h-2v-6h2v6zm0-8h-2V7h2v2z" />
                            </svg>
                            <span>Sobre Nós</span>
                        </a>
                    </div>

                    <div class="nav-item">
                        <a href="/">
                            <svg viewBox="0 0 24 24">
                                <path d="M10 20v-6h4v6h5v-8h3L12 3 2 12h3v8z" />
                            </svg>
                            <span>Home</span>
                        </a>
                    </div>

                    <div class="nav-item">
                        <a href='/usuarios/perfil-usuario/<%= usuario.id%>'>
                            <button class="nav-button">
                                <svg viewBox="0 0 24 24">
                                    <path
                                        d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm0 3c1.66 0 3 1.34 3 3s-1.34 3-3 3-3-1.34-3-3 1.34-3 3-3zm0 14.2c-2.5 0-4.71-1.28-6-3.22.03-1.99 4-3.08 6-3.08 1.99 0 5.97 1.09 6 3.08-1.29 1.94-3.5 3.22-6 3.22z" />
                                </svg>
                                <span>Perfil</span>
                            </button>
                        </a>
                    </div>
                </div>
            </div>
        </nav>
    </header>